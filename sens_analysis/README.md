# Sensitivity Analyses

This folder retains the alternative Title IX exposure specifications and
cross-validation code from the earlier analysis. These models are not part of
the paper's primary pipeline and are not sourced by `main.R`.

Run all sensitivity models and their cross-validation from the repository
root with:

```sh
Rscript sens_analysis/main.R
```

Results are written to `sens_analysis/output/`.
